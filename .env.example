# ── LLM API 配置 ──
# 从 Deepseek / OpenAI 等平台获取你的 API Key
# 然后在项目根目录 cp .env.example .env，填入你的 Key
DEEPSEEK_API_KEY=
DEEPSEEK_API_BASE=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# ── 数据库路径（默认当前目录下的 data/）──
DATABASE_PATH=./data/panel_studio.db

# ── 服务端口 ──
HOST=0.0.0.0
PORT=8000
